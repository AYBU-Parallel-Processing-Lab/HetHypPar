#include "unity.h"
#include "hhp_matrix.h"

void setUp(void) {
    // Set up test fixtures if needed
}

void tearDown(void) {
    // Clean up after tests if needed
}

// void test_matrix_creation(void) {
//     // Example test
//     struct HHPMatrix *matrix = create_matrix(3, 3);
//     TEST_ASSERT_NOT_NULL(matrix);
//     TEST_ASSERT_EQUAL_INT(3, matrix->rows);
//     TEST_ASSERT_EQUAL_INT(3, matrix->cols);
//     // Clean up
//     free_matrix(matrix);
// }

// Add more test functions here

int main(void) {
    UNITY_BEGIN();
    // RUN_TEST(test_matrix_creation);
    // Add more RUN_TEST calls for additional test functions
    return UNITY_END();
}